# 로컬: 비우면 프록시 대상은 http://127.0.0.1:8000
# Docker dev( docker-compose.dev.yml ): 컨테이너에서 자동으로 http://api:8000
# VITE_DEV_API_PROXY=http://api:8000

# 빌드 후 다른 호스트의 API를 쓸 때만 (Release Docker+Nginx 조합에서는 보통 불필요)
# VITE_API_ORIGIN=http://127.0.0.1:8000
